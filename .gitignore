# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Environment / secrets — never commit credentials
.env
.env.*
!.env.example
*.secret

# Personal MCP client configs (may contain NCCU credentials in headers)
.mcp.json
opencode.json

# Moodle session / cookie caches (credentials-equivalent)
.moodle_cookies.txt
*.cookies.txt

# Claude Code conversation exports (may contain credentials in plaintext)
20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt

# Node (auth service)
node_modules/
auth/dist/

# Logs and OS cruft
*.log
.DS_Store
